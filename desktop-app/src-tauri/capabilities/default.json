{
  "identifier": "default",
  "description": "Core WOTANN Desktop capabilities — window, app lifecycle, and path utilities only. Scoped I/O capabilities live in separate files (fs.json, shell.json, ui.json) so each grant is audited independently.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
