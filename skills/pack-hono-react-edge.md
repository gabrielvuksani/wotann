---
name: pack-hono-react-edge
description: Materializes a Hono 4 + React edge-rendered scaffold deployable to Cloudflare Workers (SSR + hydration).
type: scaffold-pack
source: wotann
materializes: hono-react-edge
license: MIT
context: fork
paths: ["**/wrangler.toml", "**/src/server.*", "**/src/client.*"]
---

# Pack: Hono + React (edge)

A minimal but functional Hono 4 + React scaffold emitted by
`wotann build` when the selected scaffold is `hono-react-edge`. The
project boots in Wrangler's local dev (`npm run dev`) and SSRs a React
landing page through Hono's `@hono/jsx` renderer with a small
client-side hydration entry. Designed for Cloudflare Workers but the
runtime is provider-agnostic edge.

## Files

### package.json

```json
{
  "name": "wotann-hono-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "4.5.3",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20240712.0",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0",
    "typescript": "5.5.3",
    "wrangler": "3.65.1"
  }
}
```

### tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "types": ["@cloudflare/workers-types"]
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

### wrangler.toml

```toml
name = "wotann-hono-edge"
main = "src/server.tsx"
compatibility_date = "2024-07-01"

[build]
command = ""

[observability]
enabled = true
```

### src/index.html

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Wotann — Hono + React (edge)</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/client.js"></script>
  </body>
</html>
```

### src/app.tsx

```tsx
import * as React from "react";

export interface AppProps {
  readonly hydrationOrigin: "edge" | "browser";
}

export function App({ hydrationOrigin }: AppProps) {
  return (
    <main style={{ fontFamily: "system-ui", padding: 24, maxWidth: 720 }}>
      <h1 style={{ fontSize: 28, fontWeight: 600 }}>
        Wotann build — Hono + React (edge)
      </h1>
      <p style={{ color: "#555" }}>
        Rendered from <code>{hydrationOrigin}</code>. SSR on Cloudflare
        Workers via Hono, hydrated by the React 18 runtime.
      </p>
      <ul>
        <li>Edge runtime — sub-10ms cold start</li>
        <li>Hono router + middleware</li>
        <li>React 18 SSR + hydration</li>
        <li>Wrangler 3 dev + deploy</li>
      </ul>
    </main>
  );
}
```

### src/server.tsx

```tsx
import { Hono } from "hono";
import { renderToString } from "react-dom/server";
import * as React from "react";
import { App } from "./app.js";

const app = new Hono();

app.get("/api/health", (c) => c.json({ ok: true, scaffold: "hono-react-edge" }));

app.get("/client.js", (c) => {
  // The bundler is expected to emit a sibling client.js; in dev we
  // serve a stub that mounts the React tree in the browser.
  c.header("content-type", "application/javascript; charset=utf-8");
  return c.body(
    `import { hydrateRoot } from "https://esm.sh/react-dom@18.3.1/client";
     import * as React from "https://esm.sh/react@18.3.1";
     import { App } from "/app.js";
     const el = document.getElementById("root");
     if (el) hydrateRoot(el, React.createElement(App, { hydrationOrigin: "browser" }));`,
  );
});

app.get("*", (c) => {
  const html = renderToString(
    React.createElement(App, { hydrationOrigin: "edge" }),
  );
  return c.html(`<!DOCTYPE html>
<html lang="en"><head><meta charset="utf-8"/>
<title>Wotann — Hono + React (edge)</title></head>
<body><div id="root">${html}</div>
<script type="module" src="/client.js"></script>
</body></html>`);
});

export default app;
```

### src/client.tsx

```tsx
import * as React from "react";
import { hydrateRoot } from "react-dom/client";
import { App } from "./app.js";

const el = document.getElementById("root");
if (el) {
  hydrateRoot(el, <App hydrationOrigin="browser" />);
}
```

### .gitignore

```
node_modules
dist
.wrangler
.env*
.wotann
.DS_Store
*.log
```

### README.md

```md
# Wotann App — Hono + React (edge)

Generated by `wotann build` using the **hono-react-edge** scaffold.

## Run locally

```bash
npm install
npm run dev
```

Wrangler will start a local edge dev server (default port 8787). Visit
the root path for the SSR'd React app, or `/api/health` for the JSON
health probe.

## Deploy to Cloudflare Workers

```bash
npx wrangler login
npm run deploy
```

## What's inside

- `src/server.tsx` — Hono app: routes, SSR, JSON API
- `src/app.tsx` — React component shared between SSR and client
- `src/client.tsx` — hydration entry
- `src/index.html` — static HTML shell (used by static-site bundlers)
- `wrangler.toml` — Cloudflare Workers config

## Provenance

Materialized from `skills/pack-hono-react-edge.md` by Wotann's build
pipeline. See `docs/build-templates/README.md` for the full flow.
```

## Provenance

WOTANN scaffold pack used by `wotann build` when the registry picks
`hono-react-edge`. Tested with Hono 4.5.x, React 18.3.x, Wrangler
3.65.x. The file list is kept in 1:1 sync with
`SCAFFOLDS[hono-react-edge].files` in `src/build/scaffold-registry.ts`,
with `app.tsx` shared between the SSR and client entries.
