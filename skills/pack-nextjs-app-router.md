---
name: pack-nextjs-app-router
description: Materializes a minimal but functional Next.js 14+ App Router scaffold (boots `npm run dev`, serves home + /api/health).
type: scaffold-pack
source: wotann
materializes: nextjs-app-router
license: MIT
context: fork
paths: ["**/next.config*", "**/app/**"]
---

# Pack: Next.js App Router

A minimal but functional Next.js 14+ scaffold emitted by `wotann build`
when the selected scaffold is `nextjs-app-router`. After materialization
the project boots with `npm install && npm run dev` and serves:

- `GET /` — landing page with Tailwind class skeleton (no Tailwind runtime
  yet — the classes are kept as a stable starting point so the design
  system can be wired in later without rewriting markup).
- `GET /api/health` — JSON `{ ok: true }`.

## Files

### package.json

```json
{
  "name": "wotann-nextjs-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "20.14.10",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0",
    "typescript": "5.5.3"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
```

### tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": false,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### next.config.ts

```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  experimental: {
    typedRoutes: true,
  },
};

export default nextConfig;
```

### app/layout.tsx

```tsx
import type { ReactNode } from "react";

export const metadata = {
  title: "Wotann App",
  description: "Generated by `wotann build` — Next.js App Router scaffold.",
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <body className="min-h-screen bg-neutral-950 text-neutral-50 antialiased">
        {children}
      </body>
    </html>
  );
}
```

### app/page.tsx

```tsx
export default function Page() {
  return (
    <main className="mx-auto max-w-3xl px-6 py-16">
      <h1 className="text-4xl font-semibold tracking-tight">
        Wotann build — Next.js App Router
      </h1>
      <p className="mt-4 text-neutral-400">
        This scaffold was emitted by <code className="font-mono">wotann build</code>.
        Edit <code className="font-mono">app/page.tsx</code> to customize the
        landing page; the health endpoint lives at{" "}
        <a href="/api/health" className="underline">
          /api/health
        </a>
        .
      </p>
      <ul className="mt-8 space-y-2 text-sm text-neutral-300">
        <li>• Server Components by default</li>
        <li>• Streaming + Suspense ready</li>
        <li>• TypeScript strict mode</li>
        <li>• Typed routes enabled</li>
      </ul>
    </main>
  );
}
```

### app/api/health/route.ts

```ts
import { NextResponse } from "next/server";

export const dynamic = "force-dynamic";
export const runtime = "nodejs";

export function GET() {
  return NextResponse.json({ ok: true, scaffold: "nextjs-app-router" });
}
```

### .gitignore

```
node_modules
.next
out
dist
.env*
.wotann
.DS_Store
*.log
```

### README.md

```md
# Wotann App — Next.js App Router

Generated by `wotann build` using the **nextjs-app-router** scaffold.

## Run locally

```bash
npm install
npm run dev
```

Open http://localhost:3000. The health probe lives at
http://localhost:3000/api/health and returns `{ "ok": true }`.

## Scripts

- `npm run dev` — start the dev server on port 3000
- `npm run build` — production build
- `npm run start` — serve the production build
- `npm run typecheck` — `tsc --noEmit`
- `npm run lint` — `next lint`

## What's inside

- `app/layout.tsx` — root layout
- `app/page.tsx` — landing page
- `app/api/health/route.ts` — health endpoint
- `next.config.ts` — minimal Next config (typed routes on)

Ready for: Tailwind, server actions, route handlers, server components.

## Provenance

Materialized from `skills/pack-nextjs-app-router.md` by Wotann's build
pipeline. See `docs/build-templates/README.md` for the full flow.
```

## Provenance

WOTANN scaffold pack used by `wotann build` when the registry picks
`nextjs-app-router`. Tested with Node 18.17+ and Next.js 14.2.x. The
file list is kept in 1:1 sync with `SCAFFOLDS[nextjs-app-router].files`
in `src/build/scaffold-registry.ts`.
